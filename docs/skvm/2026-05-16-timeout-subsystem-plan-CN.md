<!-- Chinese translation generated by Codex from 2026-05-16-timeout-subsystem-plan.md; code blocks, commands, and links are preserved. -->

# 超时子系统统一——实施计划

> **对于代理工作人员：** 所需的子技能：使用 superpowers:subagent-driven-development （推荐）或 superpowers:executing-plans 来逐个任务地实施此计划。步骤使用复选框 (`- [ ]`) 语法进行跟踪。

**目标：** 将 SkVM 的八个分散的超时统一到单个 `--timeout-ms` CLI 标志（每个命令一个）和一小组每个参与者的帮助程序后面，从而关闭 #22。

**架构：**新模块 `src/core/timeouts.ts` 包含 `TIMEOUT_DEFAULTS`（单一默认表）和五个每参与者解析器助手（`resolveTaskTimeout`、`resolveCompilerTimeout`、`resolveOptimizerTimeout`、`resolveTaskGenTimeout`、`resolveCandidateGenTimeout`）。每个命令都会将 `flags["timeout-ms"]` 解析为一个 `cliTimeoutMs` 并将其路由到适用的帮助程序。旧的驼峰命名法标记被删除； #18“您是说吗”处理程序涵盖了迁移。

**技术堆栈：** TypeScript、Bun、`src/index.ts` 和 `src/bench/index.ts` 中现有的手动 CLI、#18 中的现有 `assertKnownFlags`、#19 中的现有 `resolveTaskRuntime`。

**先决条件：** PR #19 必须在此计划开始之前合并。该计划修改 PR #19 引入的文件（`src/core/task-runtime.ts`、`test/core/task-runtime.test.ts`）并删除 PR #19 添加的 CLI 标志（`jit-optimize` 上的 `--timeoutMs`、`jit-optimize` 上的 `--maxSteps`）。如果 #19 尚未合并，请暂停此计划。

**设计文档：** `docs/skvm/2026-05-16-timeout-subsystem.md`。请参阅它以了解完整的理由；该计划执行那里的决定。

---

## 文件结构

| 小路 | 角色 |
| --- | --- |
| `src/core/timeouts.ts` | **新。** `TIMEOUT_DEFAULTS` 常量 + 5 个 `resolveX` 助手。默认超时值的单一事实来源。 |
| `test/core/timeouts.test.ts` | **新。** 所有 5 个助手的优先矩阵测试。 |
| `src/core/task-runtime.ts` | **修改。** `resolveTaskRuntime`（来自#19）将其超时一半委托给`resolveTaskTimeout`。外部形状不变。 |
| `src/compiler/types.ts` | **修改。** `CompileOptions` 获得 `timeoutMs?: number`。 |
| `src/compiler/passes/types.ts` | **修改。** `PassContext` 获得 `timeoutMs: number`（必需）。 |
| `src/compiler/index.ts` | **修改。** `compileSkill` 解析 `ctx.timeoutMs` 并传播它。 |
| `src/compiler/passes/rewrite-skill/index.ts` | **修改。** 将 `ctx.timeoutMs` 传递给 `runPass1Agentic`。 |
| `src/compiler/passes/rewrite-skill/agent.ts` | **修改。** `runPass1Agentic` 接受 `timeoutMs`；第 442 行的 `300_000` 文字被替换。 |
| `test/compiler/timeout-passthrough.test.ts` | **新。** 模拟提供者休眠；断言 Pass 1 在截止日期前中止。兼作 #22 的测试。 |
| `src/index.ts` | **修改。** 将`--timeout-ms`添加到`aot-compile`/`pipeline`/`profile`；在 `run`/`jit-optimize` 上重命名 `--timeoutMs` → `--timeout-ms`；在 `jit-optimize` 上重命名 `--maxSteps` → `--max-steps`。替换 3 个硬编码的配置文件文字（第 302、312、887 行）。到处更新帮助文本。 |
| `src/profiler/index.ts` | **修改。** 替换第 289 行的 `300_000` 文字。 |
| `src/jit-optimize/loop.ts` | **修改。** 将第 560、1060 行的 `600_000` 文字替换为 `resolveOptimizerTimeout`。接受 `RunTasksParams` 中的 `cliTimeoutMs`。 |
| `src/jit-optimize/optimizer.ts` | **修改。**删除`DEFAULT_TIMEOUT_MS`；使用`TIMEOUT_DEFAULTS.optimizer`。 |
| `src/jit-optimize/task-source.ts` | **修改。**删除`TASK_GEN_TIMEOUT_MS`；使用`resolveTaskGenTimeout`。接受`cliTimeoutMs`。 |
| `src/jit-boost/candidates.ts` | **修改。** 将第 39、338 行的后备替换为 `TIMEOUT_DEFAULTS.taskExec` / `TIMEOUT_DEFAULTS.candidateGen`。 |
| `src/bench/index.ts` | **修改。** 从`BENCH_KNOWN_FLAGS`中删除`"timeout-mult"`；删除解析；删除帮助文本。 |
| `src/bench/conditions.ts` | **修改。** 在三个调用点将 `resolveCandidateGenTimeout({ cli: cliTimeoutMs })` 传递给 `generateBoostCandidates`/`generateTemplates`。 |
| `test/cli/deprecated-flag-hints.test.ts` | **新。** 端到端烟雾：删除的标志退出非零并带有“您的意思是”建议。 |
| `test/bench/timeout-mult-removed.test.ts` | **新。** `bench --timeout-mult=2` 以非零值退出（未知标志）。 |
| `test/core/task-runtime.test.ts` | **修改。**现有断言仍然成立；测试保持不变，除非公共形状发生变化（不应该）。 |
| `test/jit-optimize/cli-timeout-args.test.ts` | **修改。** 将标志引用从 `--timeoutMs`/`--maxSteps` 重命名为 `--timeout-ms`/`--max-steps`。 |

帮助器模块故意很小——一个常量 + 五个精简函数——因为将来添加特定于参与者的 CLI 覆盖应该是严格的本地更改。编译器类型必然会影响三个文件（`CompileOptions` → `PassContext` → `runPass1Agentic`）；这是现有通道架构允许的最小管道。

---

## 任务 1 — 创建 `src/core/timeouts.ts`（基础、TDD）

**文件：**
- 创建：`src/core/timeouts.ts`
- 创建：`test/core/timeouts.test.ts`

- [ ] **第 1 步：编写失败的测试**

```ts
// test/core/timeouts.test.ts
import { describe, test, expect } from "bun:test"
import {
  TIMEOUT_DEFAULTS,
  resolveTaskTimeout,
  resolveCompilerTimeout,
  resolveOptimizerTimeout,
  resolveTaskGenTimeout,
  resolveCandidateGenTimeout,
} from "../../src/core/timeouts.ts"
import { TASK_FILE_DEFAULTS } from "../../src/core/ui-defaults.ts"

describe("TIMEOUT_DEFAULTS", () => {
  test("taskExec mirrors TASK_FILE_DEFAULTS.timeoutMs", () => {
    expect(TIMEOUT_DEFAULTS.taskExec).toBe(TASK_FILE_DEFAULTS.timeoutMs)
  })
  test("documented numeric defaults", () => {
    expect(TIMEOUT_DEFAULTS.compiler).toBe(300_000)
    expect(TIMEOUT_DEFAULTS.optimizer).toBe(600_000)
    expect(TIMEOUT_DEFAULTS.taskGen).toBe(900_000)
    expect(TIMEOUT_DEFAULTS.candidateGen).toBe(180_000)
  })
})

describe("resolveTaskTimeout", () => {
  test("cli wins outright when given", () => {
    expect(resolveTaskTimeout({ cli: 5000, task: { timeoutMs: 999 }, multiplier: 99 })).toBe(5000)
  })
  test("falls back to task value", () => {
    expect(resolveTaskTimeout({ task: { timeoutMs: 7000 } })).toBe(7000)
  })
  test("applies multiplier to task value when cli absent", () => {
    expect(resolveTaskTimeout({ task: { timeoutMs: 1000 }, multiplier: 2.5 })).toBe(2500)
  })
  test("multiplier defaults to 1 when undefined", () => {
    expect(resolveTaskTimeout({ task: { timeoutMs: 4242 } })).toBe(4242)
  })
  test("multiplier is ignored when cli is given", () => {
    expect(resolveTaskTimeout({ cli: 100, task: { timeoutMs: 1000 }, multiplier: 5 })).toBe(100)
  })
})

describe("resolveCompilerTimeout", () => {
  test("cli wins", () => {
    expect(resolveCompilerTimeout({ cli: 12345 })).toBe(12345)
  })
  test("falls back to TIMEOUT_DEFAULTS.compiler", () => {
    expect(resolveCompilerTimeout({})).toBe(TIMEOUT_DEFAULTS.compiler)
  })
})

describe("resolveOptimizerTimeout", () => {
  test("cli wins", () => {
    expect(resolveOptimizerTimeout({ cli: 22222 })).toBe(22222)
  })
  test("falls back to TIMEOUT_DEFAULTS.optimizer", () => {
    expect(resolveOptimizerTimeout({})).toBe(TIMEOUT_DEFAULTS.optimizer)
  })
})

describe("resolveTaskGenTimeout", () => {
  test("cli wins", () => {
    expect(resolveTaskGenTimeout({ cli: 33333 })).toBe(33333)
  })
  test("falls back to TIMEOUT_DEFAULTS.taskGen", () => {
    expect(resolveTaskGenTimeout({})).toBe(TIMEOUT_DEFAULTS.taskGen)
  })
})

describe("resolveCandidateGenTimeout", () => {
  test("cli wins", () => {
    expect(resolveCandidateGenTimeout({ cli: 44444 })).toBe(44444)
  })
  test("falls back to TIMEOUT_DEFAULTS.candidateGen", () => {
    expect(resolveCandidateGenTimeout({})).toBe(TIMEOUT_DEFAULTS.candidateGen)
  })
})
```

- [ ] **第 2 步：运行测试以验证它们是否失败**

运行：`bun test test/core/timeouts.test.ts`
预期：失败 — 模块 `src/core/timeouts.ts` 尚不存在。

- [ ] **步骤 3：实施模块**

```ts
// src/core/timeouts.ts
import { TASK_FILE_DEFAULTS } from "./ui-defaults.ts"

/**
 * Built-in defaults per actor (ms). Single source of truth for "how long
 * should X reasonably take if the user hasn't said otherwise."
 *
 * - taskExec: target adapter solving one task (mirrors TASK_FILE_DEFAULTS so
 *   the schema default and the actor default stay synchronized).
 * - compiler: compiler Pass 1 (rewrite-skill) agent loop.
 * - optimizer: jit-optimize per-round skill rewriter.
 * - taskGen: jit-optimize synthetic task-generation agent.
 * - candidateGen: jit-boost candidate-extraction agent.
 */
export const TIMEOUT_DEFAULTS = {
  taskExec:     TASK_FILE_DEFAULTS.timeoutMs,
  compiler:     300_000,
  optimizer:    600_000,
  taskGen:      900_000,
  candidateGen: 180_000,
} as const

/**
 * Resolve the effective timeout for one task-execution run.
 *
 * Precedence: CLI absolute > task.timeoutMs × multiplier > task.timeoutMs.
 * `multiplier` is sourced from custom-plan YAML's group-level `timeout-mult`
 * (the CLI `--timeout-mult` flag has been removed). When `cli` is given, the
 * multiplier is ignored — absolute beats relative.
 */
export function resolveTaskTimeout(opts: {
  cli?: number
  task: { timeoutMs: number }
  multiplier?: number
}): number {
  if (opts.cli !== undefined) return opts.cli
  return opts.task.timeoutMs * (opts.multiplier ?? 1)
}

export function resolveCompilerTimeout(opts: { cli?: number }): number {
  return opts.cli ?? TIMEOUT_DEFAULTS.compiler
}

export function resolveOptimizerTimeout(opts: { cli?: number }): number {
  return opts.cli ?? TIMEOUT_DEFAULTS.optimizer
}

export function resolveTaskGenTimeout(opts: { cli?: number }): number {
  return opts.cli ?? TIMEOUT_DEFAULTS.taskGen
}

export function resolveCandidateGenTimeout(opts: { cli?: number }): number {
  return opts.cli ?? TIMEOUT_DEFAULTS.candidateGen
}
```

- [ ] **第 4 步：运行测试以验证其通过**

运行：`bun test test/core/timeouts.test.ts`
预期：通过（16 次测试）。

- [ ] **第 5 步：类型检查**

运行：`bunx tsc --noEmit`
预期：干净。

- [ ] **第 6 步：提交**

```bash
git add src/core/timeouts.ts test/core/timeouts.test.ts
git commit -m "core: add timeouts module with TIMEOUT_DEFAULTS and per-actor resolvers"
```

---

## 任务 2 — 将 `resolveTaskRuntime` 的超时一半委托给 `resolveTaskTimeout`

**文件：**
- 修改：`src/core/task-runtime.ts`
- 验证：`test/core/task-runtime.test.ts`（现有，来自 PR #19；测试保持绿色）

- [ ] **第一步：确认当前形状**

运行：`grep -n "resolveTaskRuntime\|timeoutMs" src/core/task-runtime.ts`
预期：请参阅 PR #19 的实现。该函数从 CLI/任务/乘数组合中计算有效的 `timeoutMs` 和 `maxSteps`。

- [ ] **步骤2：修改`resolveTaskRuntime`以委托超时计算**

将内联超时解析分支替换为对 `resolveTaskTimeout` 的调用。 maxSteps 分支保持不变。具体来说，在`src/core/task-runtime.ts`中的超时计算处，替换：

```ts
// Before (PR #19 inline form):
const timeoutMs = overrides.timeoutMs !== undefined
  ? overrides.timeoutMs
  : task.timeoutMs * (overrides.timeoutMult ?? 1)
```

和：

```ts
// After:
import { resolveTaskTimeout } from "./timeouts.ts"
// ...
const timeoutMs = resolveTaskTimeout({
  cli: overrides.timeoutMs,
  task: { timeoutMs: task.timeoutMs },
  multiplier: overrides.timeoutMult,
})
```

（进行调整以匹配 #19 的合并代码中的确切变量名称；语义必须与之前相同。）

- [ ] **第 3 步：运行现有测试以验证没有出现任何退化**

运行：`bun test test/core/task-runtime.test.ts`
预期：通过 - PR #19 的所有 7 项测试仍为绿色。

- [ ] **第 4 步：类型检查**

运行：`bunx tsc --noEmit`
预期：干净。

- [ ] **第 5 步：提交**

```bash
git add src/core/task-runtime.ts
git commit -m "core: delegate resolveTaskRuntime's timeout half to resolveTaskTimeout"
```

---

## 任务 3 — Plumb 编译器超时（问题 #22 内部结构，TDD）

**文件：**
- 修改：`src/compiler/types.ts`
- 修改：`src/compiler/passes/types.ts`
- 修改：`src/compiler/index.ts`
- 修改：`src/compiler/passes/rewrite-skill/index.ts`
- 修改：`src/compiler/passes/rewrite-skill/agent.ts`
- 创建：`test/compiler/timeout-passthrough.test.ts`

- [ ] **第 1 步：编写失败的测试**

```ts
// test/compiler/timeout-passthrough.test.ts
import { describe, test, expect } from "bun:test"
import { mkdtempSync, writeFileSync, rmSync } from "node:fs"
import { tmpdir } from "node:os"
import path from "node:path"
import { compileSkill } from "../../src/compiler/index.ts"
import type { LLMProvider } from "../../src/providers/types.ts"
import type { TCP } from "../../src/core/types.ts"

describe("compileSkill: compiler timeout passthrough", () => {
  test("aborts Pass 1 when provider exceeds the configured timeout", async () => {
    const tmp = mkdtempSync(path.join(tmpdir(), "skvm-compiler-timeout-"))
    writeFileSync(path.join(tmp, "SKILL.md"), "# Test skill\n\nDo a thing.\n")

    let providerCallCount = 0
    const slowProvider: LLMProvider = {
      async chat() {
        providerCallCount++
        await new Promise((r) => setTimeout(r, 300))
        return { content: "", toolCalls: [], stopReason: "end_turn", usage: { input_tokens: 0, output_tokens: 0 } }
      },
    } as unknown as LLMProvider

    const tcp: TCP = {
      model: "test/dummy",
      profiledAt: new Date().toISOString(),
      schemaVersion: 1,
      summary: { l1: {}, l2: {}, l3: {} },
      details: [
        // At least one detail with a failing primitive so analyzeGaps yields gaps.
        { primitiveId: "p1", levelResults: [{ level: 1, passRate: 0, failureArtifacts: [] }], convLogDir: undefined as unknown as string },
      ],
    } as unknown as TCP

    try {
      await compileSkill({
        skillPath: path.join(tmp, "SKILL.md"),
        skillContent: "# Test skill\n",
        skillDir: tmp,
        skillName: "test",
        tcp,
        model: "test/dummy",
        harness: "bare-agent",
        timeoutMs: 50,
      }, slowProvider, { showSpinner: false })
    } catch (err) {
      // Expected: agent loop deadline trips. Specific error message is not critical;
      // we just care that it surfaces rather than running for 5 minutes.
    }

    expect(providerCallCount).toBeLessThanOrEqual(2)

    rmSync(tmp, { recursive: true, force: true })
  }, 5_000) // hard cap: if our timeout=50 didn't fire, this assertion would hang for 5min
})
```

- [ ] **第 2 步：运行测试以验证其是否失败**

运行：`bun test test/compiler/timeout-passthrough.test.ts`
预期：FAIL — `compileSkill` 尚未接受 `CompileOptions` 中的 `timeoutMs`，或者该选项被忽略且测试超时。

- [ ] **步骤 3：将 `timeoutMs?: number` 添加到 `CompileOptions`**

在 `src/compiler/types.ts` 的 `CompileOptions` 接口内部（第 94+ 行附近），附加：

```ts
export interface CompileOptions {
  // ...existing fields...
  /** Optional override for the per-pass agent-loop timeout in milliseconds.
   *  When omitted, each pass uses TIMEOUT_DEFAULTS.compiler. */
  timeoutMs?: number
}
```

- [ ] **步骤 4：将 `timeoutMs: number` 添加到 `PassContext`**

在 `src/compiler/passes/types.ts` 的 `PassContext` 接口内部（第 20+ 行左右），附加：

```ts
export interface PassContext {
  // ...existing fields...
  /** Resolved agent-loop deadline (ms) for this pass run. The orchestrator
   *  fills it from CompileOptions.timeoutMs, falling back to the per-actor
   *  default. */
  timeoutMs: number
}
```

- [ ] **步骤 5：在 `compileSkill` 中解析并传播 `timeoutMs`**

在 `src/compiler/index.ts` 中，顶部附近：

```ts
import { resolveCompilerTimeout } from "../core/timeouts.ts"
```

在构建每个 `PassContext` 的循环中（第 88 行左右），添加新字段：

```ts
const ctx: PassContext = {
  skillName,
  workDir,
  skillContent,
  tcp: opts.tcp,
  model: opts.model,
  harness: opts.harness,
  provider: wrappedProvider,
  failureContext: opts.failureContext,
  artifacts: store,
  timeoutMs: resolveCompilerTimeout({ cli: opts.timeoutMs }),
}
```

- [ ] **步骤 6：将 `ctx.timeoutMs` 穿过 `rewriteSkillPass.run`**

在`src/compiler/passes/rewrite-skill/index.ts`中，更新调用：

```ts
async run(ctx: PassContext): Promise<PassOutput> {
  const result = await runPass1Agentic(
    ctx.skillContent,
    ctx.tcp,
    ctx.provider,
    ctx.workDir,
    ctx.failureContext,
    ctx.timeoutMs,
  )
  // ...rest unchanged
}
```

- [ ] **步骤 7：使 `runPass1Agentic` 接受 `timeoutMs` 并删除硬编码文字**

在 `src/compiler/passes/rewrite-skill/agent.ts` 中，更改函数签名以接受新参数并在代理循环配置中使用它（替换第 442 行的文字）：

```ts
export async function runPass1Agentic(
  skillContent: string,
  tcp: TCP,
  provider: LLMProvider,
  workDir: string,
  failureContext: FailureContext | undefined,
  timeoutMs: number,
): Promise<Pass1Result> {
  // ...existing body unchanged until the runAgentLoop call...
  const loopResult = await runAgentLoop(
    {
      provider,
      model: tcp.model,
      tools: AGENT_TOOLS,
      executeTool,
      system,
      maxIterations: 15,
      timeoutMs,
      maxTokens: 32768,
      temperature: 0,
    },
    [{ role: "user", content: initialMessage }],
  )
  // ...rest unchanged
}
```

- [ ] **步骤 8：运行新测试以验证其通过**

运行：`bun test test/compiler/timeout-passthrough.test.ts`
预期：通过 — 通过 1 在 50 毫秒截止日期内中止；提供者呼叫计数 ≤ 2。

- [ ] **步骤 9：运行完整的编译器测试套件以验证没有回归**

运行：`bun test test/compiler/`
预期：全部通过。

- [ ] **第 10 步：类型检查**

运行：`bunx tsc --noEmit`
预期：干净。 `PassContext` 上的新必填字段可能会在其他地方显示调用站点 - 如果是这样，请提供 `resolveCompilerTimeout({})` 直到它们在后续任务中显式连接。

- [ ] **第 11 步：提交**

```bash
git add src/compiler/types.ts src/compiler/passes/types.ts src/compiler/index.ts \
        src/compiler/passes/rewrite-skill/index.ts src/compiler/passes/rewrite-skill/agent.ts \
        test/compiler/timeout-passthrough.test.ts
git commit -m "compiler: parameterize Pass 1 agent loop timeout via CompileOptions"
```

---

## 任务 4 — 将 `--timeout-ms` 添加到 `aot-compile` 和 `pipeline`

**文件：**
- 修改：`src/index.ts`（`COMPILE_KNOWN_FLAGS`、`runCompile`、`PIPELINE_KNOWN_FLAGS`、`runPipeline`、帮助文本）

- [ ] **步骤 1：将 `"timeout-ms"` 添加到 `COMPILE_KNOWN_FLAGS`**

在 `src/index.ts` 中，靠近 `COMPILE_KNOWN_FLAGS` 声明（第 522 行附近），将 `"timeout-ms"` 添加到集合中：

```ts
const COMPILE_KNOWN_FLAGS: ReadonlySet<string> = new Set([
  // ...existing entries...
  "timeout-ms",
])
```

- [ ] **步骤2：解析`runCompile`中的`--timeout-ms`**

在 `runCompile` 中的帮助文本分支之后，在 `compileSkill` 调用之前，添加解析器：

```ts
let cliCompilerTimeoutMs: number | undefined
if (flags["timeout-ms"] !== undefined) {
  const n = parseInt(flags["timeout-ms"], 10)
  if (!Number.isFinite(n) || n <= 0) {
    console.error(`aot-compile: --timeout-ms must be a positive integer (got "${flags["timeout-ms"]}")`)
    process.exit(1)
  }
  cliCompilerTimeoutMs = n
}
```

然后通过添加字段将其线程到第 708 行的现有 `compileSkill({...})` 调用中：

```ts
const result = await compileSkill({
  // ...existing fields...
  timeoutMs: cliCompilerTimeoutMs,
}, ...)
```

- [ ] **步骤 3：在 `runCompile` 中添加帮助文本行**

在 `aot-compile` 的多行帮助字符串中，添加（放置在其他 LLM/编译器选项附近）：

```
  --timeout-ms=<n>      Cap on the compiler agent loop (Pass 1, rewrite-skill)
                        while it edits SKILL.md (ms). Default: 300000.
```

- [ ] **步骤 4：对 `pipeline` 重复相同的三项更改**

在 `PIPELINE_KNOWN_FLAGS`（第 769 行附近）中，添加 `"timeout-ms"`。在 `runPipeline` 中，将 `flags["timeout-ms"]` 解析为 `cliPipelineTimeoutMs` 并将其作为 `timeoutMs: cliPipelineTimeoutMs` 传递给编译调用（第 923 行），并作为每个探针超时传递给配置文件设置分支。帮助文本：

```
  --timeout-ms=<n>      Per-agent-loop ceiling for this pipeline run (ms).
                        Applies to BOTH the profile stage's per-probe agent
                        execution AND the compiler agent loop. Each is timed
                        independently — this is a per-loop ceiling, not a
                        total wall time.
```

对于管道分析阶段接线，`runPipeline` 内部构建 `adapterConfig` 进行分析的现有调用需要来自 `cliPipelineTimeoutMs ?? TIMEOUT_DEFAULTS.taskExec` 的 `timeoutMs`。在 `src/index.ts` 顶部添加导入：

```ts
import { TIMEOUT_DEFAULTS, resolveTaskTimeout } from "./core/timeouts.ts"
```

- [ ] **步骤 5：从 CLI 对新标志进行冒烟测试**

运行：`bun run skvm aot-compile --timeout-ms=abc 2>&1 | 头-3`
预期：包含 `--timeout-ms` 和 `positive integer` 的错误消息，退出非零。

运行：`bun run skvm aot-compile --help 2>&1 | grep 超时-ms`
预期：出现新的帮助热线。

运行：`bun run skvm pipeline --help 2>&1 | grep 超时-ms`
预期：出现新的帮助热线。

- [ ] **第 6 步：类型检查并运行编译器测试**

运行：`bunx tsc --noEmit`
预期：干净。

运行：`bun test test/compiler/`
预期：全部通过。

- [ ] **第 7 步：提交**

```bash
git add src/index.ts
git commit -m "cli: add --timeout-ms to aot-compile and pipeline (closes #22)"
```

---

## 任务 5 — 将 `--timeout-ms` 添加到 `profile` 并清理配置文件阶段文字

**文件：**
- 修改：`src/index.ts`（`PROFILE_KNOWN_FLAGS`、`runProfile`，第302/312/887行，帮助文本）
- 修改：`src/profiler/index.ts`（第289行）

- [ ] **步骤 1：将 `"timeout-ms"` 添加到 `PROFILE_KNOWN_FLAGS`**

在 `src/index.ts` 第 144 行附近：

```ts
const PROFILE_KNOWN_FLAGS: ReadonlySet<string> = new Set([
  // ...existing entries...
  "timeout-ms",
])
```

- [ ] **步骤2：解析`runProfile`中的`--timeout-ms`**

在其他标志解析附近插入（第 200 行左右）：

```ts
let cliProfileTimeoutMs: number | undefined
if (flags["timeout-ms"] !== undefined) {
  const n = parseInt(flags["timeout-ms"], 10)
  if (!Number.isFinite(n) || n <= 0) {
    console.error(`profile: --timeout-ms must be a positive integer (got "${flags["timeout-ms"]}")`)
    process.exit(1)
  }
  cliProfileTimeoutMs = n
}
const probeTimeoutMs = cliProfileTimeoutMs ?? TIMEOUT_DEFAULTS.taskExec
```

（如果任务 4 未添加 `TIMEOUT_DEFAULTS` 导入，请立即添加。）

- [ ] **步骤 3：替换 `runProfile` 适配器配置块中的三个硬编码 `300_000` 文字**

在 `src/index.ts` 中：
- 第 302 行：`adapterConfig: { model: job.model, maxSteps: 25, timeoutMs: 300_000, mode: adapterMode }` → 将 `300_000` 替换为 `probeTimeoutMs`。
- 第 312 行：`adapterFactory` 箭头函数内的相同替换。
- 第887行（`runProfile`中的并行/批量分支）：相同的替换。

- [ ] **步骤 4：替换 `src/profiler/index.ts:289` 中的 `300_000` 文字**

`src/profiler/index.ts:289` 目前有：

```ts
await adapter.setup({ model, maxSteps: 25, timeoutMs: 300_000, mode: opts.adapterMode })
```

`profile()` 函数当前不接受每次调用超时。添加可选参数：

```ts
// In ProfileOptions (or the equivalent interface in profiler/index.ts) add:
timeoutMs?: number

// Then at line 289:
await adapter.setup({
  model,
  maxSteps: 25,
  timeoutMs: opts.timeoutMs ?? TIMEOUT_DEFAULTS.taskExec,
  mode: opts.adapterMode,
})
```

更新 `src/index.ts` 中的两个 `runProfile` 呼叫者，以将 `timeoutMs: probeTimeoutMs` 转发到 `profile({...})` 呼叫。

- [ ] **步骤 5：在 `runProfile` 中添加帮助文本行**

`runProfile` 的帮助字符串内部：

```
  --timeout-ms=<n>      Cap on each microbenchmark probe's adapter execution
                        (ms). Default: 120000.
```

（注意：`taskExec` 默认值 = 120 000，而不是 300 000 — 我们有意降低隐式默认值以匹配系统的其余部分。如果这在操作上很重要，则以前隐式依赖于每个探测 5 分钟的配置文件运行现在可以显式地通过 `--timeout-ms=300000`。）

- [ ] **第 6 步：冒烟测试**

运行：`bun run skvm profile --timeout-ms=abc 2>&1 | 头-3`
预期：正整数错误，退出非零。

运行：`bun run skvm profile --help 2>&1 | grep 超时-ms`
预期：新的帮助热线。

- [ ] **步骤 7：类型检查并运行配置文件测试**

运行：`bunx tsc --noEmit`
预期：干净。

运行：`bun test test/profiler/`
预期：全部通过（根据 CLAUDE.md 上下文可接受一项预先存在的片状随机测试）。

- [ ] **第 8 步：提交**

```bash
git add src/index.ts src/profiler/index.ts
git commit -m "cli: add --timeout-ms to profile and unify profile-stage probe timeouts"
```

---

## 任务 6 — 在 `skvm run` 上重命名 `--timeoutMs` → `--timeout-ms`（硬剪切）

**文件：**
- 修改：`src/index.ts`（`RUN_KNOWN_FLAGS`、`runRun`，第 366-456 行周围的帮助文本）

- [ ] **步骤1：将`RUN_KNOWN_FLAGS`中的`"timeoutMs"`替换为`"timeout-ms"`**

第 372 行：

```ts
const RUN_KNOWN_FLAGS: ReadonlySet<string> = new Set([
  "task",
  "skill",
  "model",
  "adapter",
  "workdir",
  "timeout-ms",   // was: "timeoutMs"
  "maxSteps",
  "adapter-config",
])
```

（暂时保留 `"maxSteps"` — `run` 当前不公开 `--max-steps`；只有 `jit-optimize` 公开。如果 `run` 实际上读取 `--maxSteps`，则保留它。如果没有，请将其删除。通过在 `runRun` 中搜索 `flags.maxSteps` 引用进行验证。）

- [ ] **步骤 2：更新 `runRun` 中的解析器**

在第 454 行（`timeoutMs` 提取）：

```ts
timeoutMs: flags["timeout-ms"] ? parseInt(flags["timeout-ms"], 10) : task.timeoutMs,
```

如果 `runRun` 在多个位置进行解析，请更新每个位置。

- [ ] **第 3 步：更新帮助文本**

第 394 行：

```
  --timeout-ms=<n>      Override the per-task agent execution timeout (ms).
                        This caps how long the target adapter spends solving
                        one task. Falls back to task.json's `timeoutMs`,
                        then to the built-in default (120000).
```

- [ ] **第 4 步：冒烟测试**

运行：`bun run skvm run --timeoutMs=600000 --task=/tmp/nope 2>&1 | 头-5`
预期：来自 `assertKnownFlags` 的未知标志错误，带有“您的意思是 --timeout-ms”。

运行：`bun run skvm run --help 2>&1 | grep 超时`
预期：只有新的 `--timeout-ms` 形式，没有 `--timeoutMs`。

- [ ] **第 5 步：类型检查**

运行：`bunx tsc --noEmit`
预期：干净。

- [ ] **第 6 步：提交**

```bash
git add src/index.ts
git commit -m "cli: rename --timeoutMs to --timeout-ms on skvm run (hard cut)"
```

---

## 任务 7 — `jit-optimize`：重命名标志 + 线路优化器 + 任务生成参与者

**文件：**
- 修改：`src/index.ts`（`JIT_OPTIMIZE_KNOWN_FLAGS`、`runJitOptimize`，第 1555-1600+ 行周围的帮助文本）
- 修改：`src/jit-optimize/loop.ts`（第330、560、1060行；`RunTasksParams`接口）
- 修改：`src/jit-optimize/optimizer.ts`（删除`DEFAULT_TIMEOUT_MS`）
- 修改：`src/jit-optimize/task-source.ts`（删除`TASK_GEN_TIMEOUT_MS`）
- 修改：`test/jit-optimize/cli-timeout-args.test.ts`（重命名标志引用）

- [ ] **步骤 1：重命名 `JIT_OPTIMIZE_KNOWN_FLAGS` 中的标志**

在 `src/index.ts` 中第 1555 行附近：将集合中的 `"timeoutMs"` 替换为 `"timeout-ms"`，将 `"maxSteps"` 替换为 `"max-steps"`。

- [ ] **步骤 2：更新 `runJitOptimize` 以读取新的标志名称**

将 `flags.timeoutMs` 的每次读取替换为 `flags["timeout-ms"]`，将 `flags.maxSteps` 的每次读取替换为 `runJitOptimize` 内的 `flags["max-steps"]`。验证为正整数；错误消息命名新标志。

解决 `cliTimeoutMs` 中的所有三个 actor 超时问题：

```ts
import { resolveTaskTimeout, resolveOptimizerTimeout, resolveTaskGenTimeout, TIMEOUT_DEFAULTS } from "./core/timeouts.ts"

// ...inside runJitOptimize, after parsing cliTimeoutMs and cliMaxSteps:

await runTasks({
  // ...existing params...
  cliTimeoutMs,     // forwards to per-task resolveTaskTimeout in loop.ts
  cliMaxSteps,
  optimizerTimeoutMs: resolveOptimizerTimeout({ cli: cliTimeoutMs }),
  taskGenTimeoutMs: resolveTaskGenTimeout({ cli: cliTimeoutMs }),
})
```

- [ ] **步骤 3：更新 `runJitOptimize` 中的帮助文本**

将任何现有的 `--timeoutMs`/`--maxSteps` 帮助热线替换为：

```
  --timeout-ms=<n>      Per-agent-loop ceiling for this jit-optimize run (ms).
                        Applies to:
                          - each per-task adapter execution (default: 120000)
                          - each round's optimizer agent (default: 600000)
                          - the synthetic task-gen agent if used (default: 900000)
                        Each agent loop is timed independently — this is a
                        per-loop ceiling, not a total wall time.
  --max-steps=<n>       Override max steps for the adapter.
```

- [ ] **步骤4：在`src/jit-optimize/loop.ts`中扩展`RunTasksParams`**

将两个新的可选字段添加到 params 接口：

```ts
export interface RunTasksParams {
  // ...existing fields...
  optimizerTimeoutMs?: number
  taskGenTimeoutMs?: number
}
```

- [ ] **步骤 5：替换第 560 行和 1060 行处的硬编码 `600_000`**

这两个站点都是对 `runOptimizer({ ..., timeoutMs: 600_000, ... })` （或类似）的调用。替换为解析值：

```ts
// At line 560:
timeoutMs: params.optimizerTimeoutMs ?? TIMEOUT_DEFAULTS.optimizer,

// At line 1060:
timeoutMs: params.optimizerTimeoutMs ?? TIMEOUT_DEFAULTS.optimizer,
```

在 `loop.ts` 顶部添加导入：

```ts
import { TIMEOUT_DEFAULTS } from "../core/timeouts.ts"
```

- [ ] **第 6 步：替换第 330 行的后备**

当前：`timeoutMs: config.targetAdapter.adapterConfig?.timeoutMs ?? 300_000`。
更改为：`timeoutMs: config.targetAdapter.adapterConfig?.timeoutMs ?? TIMEOUT_DEFAULTS.taskExec`。

（相同的概念默认值，仅来自中央表。）

- [ ] **步骤7：删除`optimizer.ts`中的`DEFAULT_TIMEOUT_MS`**

在 `src/jit-optimize/optimizer.ts` 中：

```ts
// Remove:
const DEFAULT_TIMEOUT_MS = 600_000

// Update the fallback in runOptimizer:
const timeoutMs = config.timeoutMs ?? TIMEOUT_DEFAULTS.optimizer

// Add the import:
import { TIMEOUT_DEFAULTS } from "../core/timeouts.ts"
```

- [ ] **步骤8：删除`task-source.ts`中的`TASK_GEN_TIMEOUT_MS`**

在 `src/jit-optimize/task-source.ts` 中：

```ts
// Remove:
const TASK_GEN_TIMEOUT_MS = 15 * 60 * 1000

// At every call site that previously used TASK_GEN_TIMEOUT_MS:
//   - If the call site receives a config object that can carry cliTimeoutMs,
//     use resolveTaskGenTimeout({ cli: cfg.cliTimeoutMs }).
//   - Otherwise use TIMEOUT_DEFAULTS.taskGen directly.
import { TIMEOUT_DEFAULTS, resolveTaskGenTimeout } from "../core/timeouts.ts"
```

如果有干净的管道，则将 `taskGenTimeoutMs` 从 `RunTasksParams` 向下线程到 `loop.ts` 中的任务源调用；如果没有，请使用默认值。

- [ ] **步骤 9：更新 `test/jit-optimize/cli-timeout-args.test.ts`**

重命名测试参数中的每个 `--timeoutMs` → `--timeout-ms` 和每个 `--maxSteps` → `--max-steps`。有关标志行为的断言仍应保持不变。

- [ ] **步骤 10：运行 jit 优化测试**

运行：`bun test test/jit-optimize/`
预期：全部通过（PR #19 的基线为 164，现在使用重命名的标志）。

- [ ] **步骤 11：对已弃用的表单进行冒烟测试并获得提示**

运行：`bun run skvm jit-optimize --timeoutMs=1000 --skill=/tmp/nope 2>&1 | 头-5`
预期：未知标志错误，“您的意思是 --timeout-ms”。

运行：`bun run skvm jit-optimize --maxSteps=10 --skill=/tmp/nope 2>&1 | 头-5`
预期：未知标志错误，“您的意思是 --max-steps”。

- [ ] **第 12 步：类型检查**

运行：`bunx tsc --noEmit`
预期：干净。

- [ ] **第 13 步：提交**

```bash
git add src/index.ts src/jit-optimize/loop.ts src/jit-optimize/optimizer.ts \
        src/jit-optimize/task-source.ts test/jit-optimize/cli-timeout-args.test.ts
git commit -m "jit-optimize: rename to kebab flags and wire optimizer/task-gen timeouts"
```

---

## 任务 8 - `bench`：删除 `--timeout-mult` CLI 标志并连接候选生成

**文件：**
- 修改：`src/bench/index.ts`（`BENCH_KNOWN_FLAGS`，解析，帮助文本）
- 修改：`src/bench/conditions.ts`（三个候选生成调用站点）
- 修改：`src/jit-boost/candidates.ts`（替换第39、338行的后备）
- 创建：`test/bench/timeout-mult-removed.test.ts`

- [ ] **第 1 步：编写失败的去除烟雾测试**

```ts
// test/bench/timeout-mult-removed.test.ts
import { describe, test, expect } from "bun:test"
import { spawnSync } from "node:child_process"
import path from "node:path"

describe("bench --timeout-mult", () => {
  test("is rejected as unknown flag", () => {
    const result = spawnSync(
      "bun",
      ["run", path.resolve(__dirname, "../../src/index.ts"), "bench", "--timeout-mult=2"],
      { encoding: "utf8" },
    )
    expect(result.status).not.toBe(0)
    expect(result.stderr.toLowerCase()).toContain("unknown flag")
    expect(result.stderr).toContain("--timeout-mult")
  })
})
```

- [ ] **第 2 步：运行测试以验证其是否失败**

运行：`bun test test/bench/timeout-mult-removed.test.ts`
预期：失败 - 该标志仍然可以识别（因为 PR #19 的解析器仍然读取它）。

- [ ] **步骤 3：从 `BENCH_KNOWN_FLAGS` 中删除 `"timeout-mult"`**

在 `src/bench/index.ts` 第 30 行左右：

```ts
// Before:
"jit-runs", "timeout-mult", "max-steps", "judge-model", "compiler-model",
// After:
"jit-runs", "max-steps", "judge-model", "compiler-model",
```

- [ ] **步骤4：删除`--timeout-mult`解析块**

在第 124 行附近，删除：

```ts
timeoutMult: flags["timeout-mult"] ? parseFloat(flags["timeout-mult"]) : CLI_DEFAULTS.timeoutMult,
```

替换为：

```ts
timeoutMult: CLI_DEFAULTS.timeoutMult,
```

（`BenchRunConfig.timeoutMult` 保留在该类型中，因为自定义计划 YAML 仍然使用它；只有 CLI 源已失效。）

- [ ] **步骤 5：删除 `--timeout-mult` 的帮助文本行**

在第545行左右，删除：

```
  --timeout-mult=<n>     Multiply task timeouts (default: ${CLI_DEFAULTS.timeoutMult.toFixed(1)})
```

并从 `--timeout-ms` 帮助文本中删除对它的任何交叉引用（PR #19 添加了一行“在设置 --timeout-ms 时忽略”——删除该行，因为该标志不再存在）。

- [ ] **第 6 步：运行删除测试以验证其现在是否通过**

运行：`bun test test/bench/timeout-mult-removed.test.ts`
预期：PASS — 未知标志错误。

- [ ] **步骤 7：在 `bench/conditions.ts` 中连接候选生成超时**

找到 `src/bench/conditions.ts` 中的三个调用站点：
- 第 725 行：`await generateTemplates(genResult.candidates, genResult.snippets, skillDir, outputDir)`
- 第 730 行：`await generateBoostCandidates(skillDir, outputDir)`
- 第 744 行：`await generateBoostCandidates(skillDir, outputDir)`

更新每个以通过已解决的候选生成超时。周围的范围已经可以访问工作台配置；找到 `cliTimeoutMs`（由 PR #19 添加到 `BenchRunConfig`）：

```ts
import { resolveCandidateGenTimeout } from "../core/timeouts.ts"

// At each call site:
const candidateGenTimeoutMs = resolveCandidateGenTimeout({ cli: config.cliTimeoutMs })
await generateTemplates(
  genResult.candidates, genResult.snippets, skillDir, outputDir,
  { timeoutMs: candidateGenTimeoutMs },
)
await generateBoostCandidates(skillDir, outputDir, { timeoutMs: candidateGenTimeoutMs })
```

（如果 `config.cliTimeoutMs` 在调用站点不能直接使用，请通过周围的函数签名来探查它。）

- [ ] **步骤 8：替换 `src/jit-boost/candidates.ts` 中的两个后备**

第 39 行：

```ts
// Before:
const timeoutMs = opts?.timeoutMs ?? TASK_FILE_DEFAULTS.timeoutMs
// After:
const timeoutMs = opts?.timeoutMs ?? TIMEOUT_DEFAULTS.taskExec
```

第 338 行：

```ts
// Before:
const timeoutMs = opts?.timeoutMs ?? 180_000
// After:
const timeoutMs = opts?.timeoutMs ?? TIMEOUT_DEFAULTS.candidateGen
```

在顶部添加导入：

```ts
import { TIMEOUT_DEFAULTS } from "../core/timeouts.ts"
```

（如果其他代码引用了 `TASK_FILE_DEFAULTS` 导入，则可以保留它；仅当它变得不使用时才将其删除。）

- [ ] **步骤 9：运行基准测试**

运行：`bun test test/bench/`
预期：全部通过。

- [ ] **第 10 步：类型检查**

运行：`bunx tsc --noEmit`
预期：干净。

- [ ] **第 11 步：提交**

```bash
git add src/bench/index.ts src/bench/conditions.ts src/jit-boost/candidates.ts \
        test/bench/timeout-mult-removed.test.ts
git commit -m "bench: remove --timeout-mult CLI flag and wire candidate-gen via TIMEOUT_DEFAULTS"
```

---

## 任务 9 — 横切已弃用标志提示烟雾 + 最终验证

**文件：**
- 创建：`test/cli/deprecated-flag-hints.test.ts`

- [ ] **第 1 步：编写横切烟雾测试**

```ts
// test/cli/deprecated-flag-hints.test.ts
import { describe, test, expect } from "bun:test"
import { spawnSync } from "node:child_process"
import path from "node:path"

const cli = path.resolve(__dirname, "../../src/index.ts")

function run(args: string[]): { code: number | null; stderr: string } {
  const r = spawnSync("bun", ["run", cli, ...args], { encoding: "utf8" })
  return { code: r.status, stderr: r.stderr }
}

describe("deprecated CLI flag → did-you-mean hint", () => {
  test("skvm run --timeoutMs suggests --timeout-ms", () => {
    const { code, stderr } = run(["run", "--timeoutMs=1000", "--task=/tmp/x"])
    expect(code).not.toBe(0)
    expect(stderr).toContain("--timeoutMs")
    expect(stderr).toContain("--timeout-ms")
  })

  test("skvm jit-optimize --timeoutMs suggests --timeout-ms", () => {
    const { code, stderr } = run(["jit-optimize", "--timeoutMs=1000", "--skill=/tmp/x"])
    expect(code).not.toBe(0)
    expect(stderr).toContain("--timeoutMs")
    expect(stderr).toContain("--timeout-ms")
  })

  test("skvm jit-optimize --maxSteps suggests --max-steps", () => {
    const { code, stderr } = run(["jit-optimize", "--maxSteps=10", "--skill=/tmp/x"])
    expect(code).not.toBe(0)
    expect(stderr).toContain("--maxSteps")
    expect(stderr).toContain("--max-steps")
  })

  test("skvm bench --timeout-mult is rejected with no suggestion", () => {
    const { code, stderr } = run(["bench", "--timeout-mult=2"])
    expect(code).not.toBe(0)
    expect(stderr.toLowerCase()).toContain("unknown flag")
    expect(stderr).toContain("--timeout-mult")
  })
})
```

- [ ] **第 2 步：运行测试**

运行：`bun test test/cli/deprecated-flag-hints.test.ts`
预期：通过（4 次测试）。

- [ ] **第 3 步：运行完整套件**

运行：`bun test`
预期：每个现有测试仍然通过，加上任务 1、3、8、9 添加的新测试。

如果任何测试失败：
- 对于直接引用旧标志名称的测试（最有可能在 `test/bench/` 和 `test/jit-optimize/` 下），请将引用更新为新名称。
- 不要放松断言——标志命名意图是迁移的重点。

- [ ] **第 4 步：最终类型检查**

运行：`bunx tsc --noEmit`
预期：干净。

- [ ] **步骤 5：Grep 查找任何剩余的硬编码超时文字**

运行：`grep -rn "300_000\|600_000\|900_000\|180_000\|TASK_GEN_TIMEOUT_MS\|DEFAULT_TIMEOUT_MS" src/`
预期：每个剩余的命中要么在 `src/core/timeouts.ts` 本身（默认表）内，要么在明确超出范围的基础设施文件内（子进程启动/sidecar/握手/openclaw poll cap/provider retry backoff/tool-level），或者在注释内。解决设计文档类别 1-6 中代理循环超时问题的真实调用站点上不应出现任何点击。

如果仍然存在任何非基础设施命中，请在提交之前将其替换为适当的 `TIMEOUT_DEFAULTS.*` 查找或 `resolveX` 调用。

- [ ] **第 6 步：提交并准备 PR**

```bash
git add test/cli/deprecated-flag-hints.test.ts
git commit -m "tests: cross-cutting smoke for deprecated timeout/step flag rejection"
```

- [ ] **第 7 步：起草 PR 描述**

打开标题为 `core: unify timeout flags as --timeout-ms across all commands` 且正文引用 `Closes #22`、`Refs #18` 和设计文档的 PR。明确指出 PR 删除了刚刚添加的 `--timeoutMs`/`--maxSteps` 标志 PR #19；这种逆转是故意的，设计文档解释了原因。

---

## 自我检讨

交接前：

- **规格覆盖范围**：
  - 默认表 → 任务 1。
  - 帮助程序 API → 任务 1。
  - `resolveTaskRuntime` 委托 → 任务 2。
  - 编译器超时（问题 #22）→ 任务 3。
  - `aot-compile`/`pipeline` 上的 CLI `--timeout-ms` → 任务 4。
  - `profile` 上的 CLI `--timeout-ms` + 硬编码 300_000 清理 → 任务 5。
  - 在 `run` 上重命名 `--timeoutMs` → 任务 6。
  - 在 `jit-optimize` + 线路优化器/任务生成参与者上重命名 `--timeoutMs`/`--maxSteps` → 任务 7。
  - 删除 `bench` 上的 `--timeout-mult` CLI + 连线候选生成 + 清理 jit-boost Candidates.ts → 任务 8。
  - 横切弃用标志测试 + 最终清理 grep → 任务 9。

- **占位符扫描**：不保留“TBD”/“TODO”/“酌情”。任务 8 第 7 步中的 `(If config.cliTimeoutMs is not directly available, plumb it through)` 是有条件的指导，而不是占位符 - 它取决于 PR #19 的合并形状是什么样子，并为实施者提供了规则（摸清它），而不是模糊的姿态。

- **类型一致性**：`TIMEOUT_DEFAULTS`、`resolveTaskTimeout`、`resolveCompilerTimeout`、`resolveOptimizerTimeout`、`resolveTaskGenTimeout`、`resolveCandidateGenTimeout` — 通篇使用相同的名称。 `BenchRunConfig` 和 `RunTasksParams` 上的 `cliTimeoutMs` 字段与 PR #19 的命名相匹配。 `CompileOptions.timeoutMs` 和 `PassContext.timeoutMs` 在任务 3、4、5 中保持一致。

- **顺序依赖性**：任务 1（基础）是其他一切的硬性先决条件。任务 3（编译器管道）是任务 4（公开使用它的 CLI 标志）的先决条件。任务 2（委托 `resolveTaskRuntime`）是独立的，可以在任务 1 之后去任何地方；提前放置，以便计划的其余部分看到统一的形状。任务 4-8 是相互独立的，如果作为单独的子 PR 进行审查，则可以并行化（不推荐 - 保留为统一叙述的一个 PR）。任务 9 必须放在最后，因为它验证清理状态。
